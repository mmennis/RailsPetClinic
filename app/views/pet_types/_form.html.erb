<%= form_for(@pet_type) do |f| %>
  <% if @pet_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pet_type.errors.count, "error") %> prohibited this pet_type from being saved:</h2>

      <ul>
      <% @pet_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
